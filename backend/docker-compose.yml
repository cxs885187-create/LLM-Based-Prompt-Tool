version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aigc-experiment-backend
    ports:
      - "8001:8001"
    env_file:
      - .env
    restart: unless-stopped
